const NearbyStopsList = ({
  selectedStop,
  stops,
}: {
  selectedStop: StopPlace;
  stops: ScheduledStopPoint[];
}) => {
  console.log(stops);
  return <ul></ul>;
};

export default NearbyStopsList;
